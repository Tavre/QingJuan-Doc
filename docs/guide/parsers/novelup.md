# Novelup 解析器

| 属性 | 内容 |
| --- | --- |
| 插件 ID | `novelup` |
| 版本 | `1.0.0` |
| 匹配域名 | `novelup.plus` |
| 作品类型 | 长小说 |
| 默认状态 | 启用 |

## 支持能力

Novelup 解析器读取公开作品目录与章节。当前没有内置搜索能力，请粘贴作品页面链接导入。

站点保护策略变化时，解析器会返回明确诊断，而不会把错误页面保存成章节正文。遇到失败时请先确认链接可从后端所在网络正常打开。

[返回解析器总览](./index.md)
