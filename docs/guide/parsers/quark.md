# 夸克小说解析器

| 属性 | 内容 |
| --- | --- |
| 插件 ID | `quark` |
| 版本 | `1.0.0` |
| 匹配域名 | `shuqi.com`、`novel.quark.cn` |
| 作品类型 | 长小说 |
| 默认状态 | 启用 |

## 支持能力

夸克小说解析器通过书旗网页内核提供匿名公开搜索、作品信息、目录和免费章节解析，并支持“边看边下”。在搜索页选择 **夸克** 后，可按书名或作者查找作品并加入书架。

解析器只保存明确标记为免费且返回完整正文的章节，不接入付费章节、登录 Cookie、账号书架，也不会用试读片段伪装完整章节。

停用 `quark` 后，后端会在网络请求前拒绝新的夸克搜索、预览与章节抓取；已经缓存的章节不受影响。

## v1.7.1 稳定性改进

- 目录请求遇到限流或 HTTP 502 / 503 / 504 临时网关错误时会进行有界重试；
- 同一作品的完整目录会短时缓存在当前后端进程内，并合并并发请求，供预览、导入和首次按需阅读复用；
- Android 从搜索结果加入书架使用后台任务，不再同步等待长目录请求；
- HTML 错误页不会原样显示给用户，公开错误只保留阶段和 HTTP 状态。

这些改动不扩大访问范围：缓存不包含账号状态，解析器仍只读取匿名公开的免费完整章节。

[返回解析器总览](./index.md)
